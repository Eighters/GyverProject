language: php

sudo: false

cache:
  directories:
    - $HOME/.composer/cache
    - node_modules

php:
    - 5.5.9

before_script:
    # Set config files
    - cp app/config/parameters.yml.dist app/config/parameters.yml
    - cp app/phpunit.xml.dist app/phpunit.xml

    # Npm dependencies
    - npm install

    # Bower dependencies
    - ./node_modules/bower/bin/bower install

    # Fix composer rate limiting issue.
    - if [ -n "$github_api_rate" ]; then composer config github-oauth.github.com ${github_api_rate}; fi;

    # Php dependencies
    - composer self-update
    - composer install --prefer-dist --no-interaction

    # DataBase config
    - php app/console doctrine:database:create --env=test
    - php app/console doctrine:schema:create --env=test
    - php app/console doctrine:fixtures:load -n --env=test

    # Compile Assets
    - ./node_modules/gulp/bin/gulp.js build

script:
    - bin/phpunit -c app/phpunit.xml

notifications:
    slack: techgamey2team1:yKi6SZOIGGUBHt3PcnkUSvG9
    email:
        - gauvin.thibaut83@gmail.com
        - rbilly.mail@gmail.com
        - dalleau.romain@gmail.com
